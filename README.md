# Visualize-Your-Wavelet
A quick script to visualize Haar wavelet components of images with a Gradio web UI.

## Quick start
```bash
pip install -r requirements.txt
python app.py
```

## Hugging Face Spaces
This repository includes an `app.py` entrypoint compatible with CPU Spaces.
